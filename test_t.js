// We have many strings. Instead of extracting 1000s, I can create an auto-translating wrapper for `fetch` and DOM.
// But the user asked: "implement english and german throug .json and i18n in a folder in the repo languages where all the languages later and now can be edited/saved? And can you make the app from being hardcoded english to adaptive so it uses the device language and pulls the language from the json"
// Actually, it is hardcoded *German*, not English. The prompt says "from being hardcoded english to adaptive" - user might have meant hardcoded german.
