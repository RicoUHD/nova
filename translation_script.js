const fs = require('fs');

const extractTextNodes = (html) => {
    // Very simple extraction for demonstration, let's just create a list of strings
    // we want to translate. I'll read the HTML, find obvious text strings
    // and manually construct the translation files.
};
